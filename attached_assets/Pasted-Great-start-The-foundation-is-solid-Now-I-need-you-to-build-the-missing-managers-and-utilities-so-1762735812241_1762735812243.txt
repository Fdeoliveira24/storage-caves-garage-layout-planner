Great start! The foundation is solid. Now I need you to build the missing managers and utilities 
so the app can actually run. 

Build these files in this exact order:

PHASE 1 - Utils (Build First):
1. js/utils/helpers.js - Must include these functions:
   - distance(x1, y1, x2, y2)
   - pxToFeet(px) using Config.PX_PER_FOOT
   - feetToPx(ft) using Config.PX_PER_FOOT
   - feetToMeters(ft)
   - formatNumber(num, decimals)
   - generateId(prefix)

2. js/utils/storage.js - localStorage wrapper:
   - save(key, data) - stringify and save
   - load(key) - load and parse JSON
   - remove(key)

3. js/utils/bounds.js - Boundary checking:
   - isWithinBounds(item, floorPlan)
   - constrainToBounds(item, floorPlan)
   - checkEntryZone(item, floorPlan) - bottom 12% of floor plan

4. js/utils/geometry.js - Geometric calculations:
   - calculateArea(points)
   - rotatePoint(x, y, angle)
   - getBoundingBox(points)

5. js/utils/validation.js - Input validation:
   - validateFloorPlan(data)
   - validateItem(data)
   - validateDimensions(width, height)

PHASE 2 - UI Component:
6. js/ui/Modal.js - Modal dialogs:
   - showPrompt(title, message) - returns Promise with input value
   - showConfirm(title, message) - returns Promise with boolean
   - showSuccess(message) - show success toast
   - showError(message) - show error toast

PHASE 3 - Managers (Build in this order):
7. js/managers/HistoryManager.js
   - Constructor: (state, eventBus)
   - save() - save current state to history stack
   - undo() - go back one state
   - redo() - go forward one state
   - Max 50 states (Config.MAX_HISTORY)

8. js/managers/CanvasManager.js
   - Constructor: (canvasId, state, eventBus)
   - init() - initialize Fabric.js canvas
   - drawFloorPlan(floorPlan) - draw floor plan rectangle
   - addItem(itemDef, x, y) - add item to canvas
   - zoomIn(), zoomOut(), resetZoom()
   - clear() - clear canvas

9. js/managers/FloorPlanManager.js
   - Constructor: (state, eventBus, canvasManager)
   - setFloorPlan(id) - set active floor plan
   - getAllFloorPlans() - get all floor plans from FloorPlans.js
   - Draw floor plan with entry zone (bottom 12%, red tinted area)

10. js/managers/ItemManager.js
    - Constructor: (state, eventBus, canvasManager)
    - addItem(itemId, x, y) - add item to canvas and state
    - removeItem(itemId) - remove item
    - duplicateItem(itemId) - duplicate item with 20px offset
    - CRITICAL: Items must be VERTICAL (lengthFt = height, widthFt = width)
    - CRITICAL: Text labels NEVER rotate (always angle = 0)

11. js/managers/SelectionManager.js
    - Constructor: (state, eventBus, canvasManager)
    - getSelection() - get selected items from canvas
    - deleteSelected() - delete selected items
    - duplicateSelected() - duplicate with offset
    - rotateSelected(angle) - rotate items
    - copySelected() - copy to clipboard
    - pasteSelected() - paste from clipboard
    - selectAll() - select all items
    - deselectAll() - clear selection
    - bringToFront() - z-order front
    - sendToBack() - z-order back
    - moveSelected(dx, dy) - nudge items

12. js/managers/ExportManager.js
    - Constructor: (state, eventBus, canvasManager)
    - exportJSON() - export state as JSON file
    - exportPNG(scale) - export canvas as PNG (use html2canvas)
    - exportPDF() - export as PDF (use jsPDF)
    - generateThumbnail() - create thumbnail for save

IMPORTANT REMINDERS:
- All managers receive (state, eventBus, canvasManager) in constructor
- Items are stored with CENTER coordinates (originX: 'center', originY: 'center')
- Items are VERTICAL by default (lengthFt = height in pixels, widthFt = width)
- Text labels stay upright (angle always 0)
- Use Config.PX_PER_FOOT for all conversions (1 foot = 10 pixels)
- Entry zone is bottom 12% of floor plan (Config.ENTRY_ZONE_PERCENTAGE)
- Use Fabric.js for canvas (already loaded in index.html)
- Use Turf.js for geometry (already loaded in index.html)

Build these files following the exact architecture patterns used in State.js and EventBus.js.
Use proper error handling, clear comments, and professional code structure.