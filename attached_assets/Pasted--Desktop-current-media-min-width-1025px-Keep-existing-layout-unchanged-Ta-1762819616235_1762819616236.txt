/* Desktop (current) */
@media (min-width: 1025px) {
  /* Keep existing layout unchanged */
}

/* Tablet Landscape (1024px - 768px) */
@media (max-width: 1024px) {
  /* Collapsible sidebar, full-width toolbar */
}

/* Tablet Portrait (767px - 481px) */
@media (max-width: 767px) {
  /* Overlay sidebar, simplified toolbar, stack controls */
}

/* Mobile (max-width: 480px) */
@media (max-width: 480px) {
  /* Bottom navigation, floating toolbar, drawer sidebar */
}
```

---

## 🖥️ TABLET LAYOUT (768px - 1024px)

### Header
- **Keep fixed at top** (56px height)
- **Add hamburger menu** button (left side) to toggle sidebar
- **App title centered** or left-aligned
- **Zoom control** moved to header right (percentage only)

### Sidebar
- **Collapsible** via hamburger menu
- **Overlay mode** on smaller tablets (< 900px)
- **Slide in from left** with backdrop blur
- **280px width maintained**
- **Swipe to close** gesture support

### Toolbar
- **Single row** maintained
- **Horizontal scroll** if needed (with fade indicators)
- **Group buttons remain** but may compact
- **Dropdowns adapt** to touch (larger hit areas)

### Canvas
- **Full width** when sidebar collapsed
- **Touch gestures:**
  - Pinch to zoom (two fingers)
  - Pan/drag (one finger when no item selected)
  - Tap to select items
  - Long-press for context menu

### Info Bar
- **Bottom fixed** (40px height)
- **Horizontal scroll** if overflows
- **Fewer segments** shown (hide less critical info)

---

## 📱 MOBILE LAYOUT (< 480px)

### Header (Compact)
```
[☰ Menu] [Garage Planner] [⋮ More]
```
- **48px height**
- **Hamburger left** → Opens sidebar drawer
- **Title centered** (truncated if needed)
- **More menu right** → Quick actions (New, Save, Export)

### Sidebar (Full-Screen Drawer)
- **Full-screen overlay** with backdrop
- **Slide in from left** with smooth animation
- **Close button top-right**
- **Tabs stack vertically** or remain horizontal
- **Floor plan cards** remain vertical list
- **Item palette** grid adjusts to 2 columns max

### Toolbar (Bottom Floating)
```
┌─────────────────────────────────────┐
│ [Undo] [Redo] [Delete] [⋯ More]    │ ← Floating
└─────────────────────────────────────┘